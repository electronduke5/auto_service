import 'package:flutter/material.dart';

class EditAutopartPage extends StatelessWidget {
  const EditAutopartPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
